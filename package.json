{
  "name": "tmdb-catalogue-browser",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
  },
  "scripts": {
    "build:css": "postcss src/main.scss -o src/main-generated.css",
    "watch:css": "postcss src/main.scss -o src/main-generated.css --watch",
    "watch": "npm run start",
    "start": "npm run watch:css & react-scripts start",
    "build": "npm run build:css && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
  }
}
